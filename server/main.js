import { Meteor } from 'meteor/meteor';

import "../imports/api/demand"
import "../imports/api/materials"
import "../imports/api/recipes"

Meteor.startup(() => {
  // code to run on server at startup

});
